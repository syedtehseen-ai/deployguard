apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deployguard.fullname" . }}-test-connection"
  labels:
    {{- include "deployguard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deployguard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
